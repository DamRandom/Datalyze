"use client";

import React from "react";
import { Bar, Pie } from "react-chartjs-2";
import {
  Chart as ChartJS,
  ArcElement,
  Tooltip,
  Legend,
  CategoryScale,
  LinearScale,
  BarElement,
} from "chart.js";
import { motion } from "framer-motion"; // Para animaciones

ChartJS.register(ArcElement, Tooltip, Legend, CategoryScale, LinearScale, BarElement);

const predictions = [71, 78, 94, 89, 332]; // Datos simulados de las predicciones generadas por el modelo

const Predictions = () => {
  const barData = {
    labels: ["Variable 1", "Variable 2", "Variable 3", "Variable 4", "Sum (V12)"],
    datasets: [
      {
        label: "Predictions",
        data: predictions,
        backgroundColor: ["#1E3A8A", "#2563EB", "#4B5563", "#1E40AF", "#94A3B8"], // Tonos fríos de azul y gris
        hoverBackgroundColor: ["#3B82F6", "#60A5FA", "#9CA3AF", "#3B82F6", "#CBD5E1"],
      },
    ],
  };

  const pieData = {
    labels: ["Variable 1", "Variable 2", "Variable 3", "Variable 4"],
    datasets: [
      {
        data: predictions.slice(0, 4),
        backgroundColor: ["#1E3A8A", "#2563EB", "#4B5563", "#1E40AF"], // Tonos fríos de azul y gris
        hoverBackgroundColor: ["#3B82F6", "#60A5FA", "#9CA3AF", "#3B82F6"],
      },
    ],
  };

  return (
    <section className="min-h-screen bg-[#f9fafb] py-16">
      <div className="container mx-auto px-6 lg:px-12">
        {/* Header */}
        <header className="mb-12 text-center">
          <h1 className="text-4xl font-bold text-[#1E293B] mb-4">Predictions Overview</h1>
          <p className="text-lg text-[#475569] max-w-3xl mx-auto text-justify">
            Below is a visual representation of the predictions generated by the model, using a test dataset as an example. These charts and tables will help you understand how the model makes its predictions across multiple variables.
          </p>
        </header>

        {/* Predictions Charts */}
        <section>
          <h2 className="text-2xl font-semibold text-[#1E293B] mb-4">Generated Predictions</h2>
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
            {/* Bar Chart */}
            <motion.div
              className="bg-white shadow-md rounded-lg p-6"
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ duration: 1 }}
            >
              <h3 className="text-lg font-semibold text-[#1E293B] mb-4">Prediction Comparison</h3>
              <p className="text-[#475569] mb-6 text-justify">
                The bar chart below shows the predicted values for different variables, including the sum of all variables (V12). The data is derived from the AutoARIMA model applied to a synthetic dataset, which allows us to observe how each variable contributes to the overall prediction.
              </p>
              <Bar data={barData} options={{ responsive: true }} />
            </motion.div>

            {/* Pie Chart */}
            <motion.div
              className="bg-white shadow-md rounded-lg p-6"
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ duration: 1 }}
            >
              <h3 className="text-lg font-semibold text-[#1E293B] mb-4">Relative Contribution</h3>
              <p className="text-[#475569] mb-6 text-justify">
                The pie chart illustrates the relative contribution of each variable to the total prediction (V12). This provides a quick overview of how each individual prediction influences the overall forecast.
              </p>
              <Pie data={pieData} options={{ responsive: true }} />
            </motion.div>
          </div>

          {/* Table */}
          <motion.div
            className="bg-white shadow-md rounded-lg mt-8 p-6 overflow-x-auto"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ duration: 1 }}
          >
            <h3 className="text-lg font-semibold text-[#1E293B] mb-4">Predicted Values</h3>
            <p className="text-[#475569] mb-6 text-justify">
              The table below displays the predicted values for each variable, along with the sum of these predictions (V12). These values were generated using the AutoARIMA model on a test dataset.
            </p>
            <table className="table-auto w-full text-sm text-[#475569]">
              <thead>
                <tr className="bg-[#f3f4f6]">
                  <th className="p-4 text-left">Variable</th>
                  <th className="p-4 text-left">Prediction</th>
                </tr>
              </thead>
              <tbody>
                {predictions.map((value, index) => (
                  <tr key={index} className="border-t">
                    <td className="p-4">Variable {index + 1}</td>
                    <td className="p-4">{value}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </motion.div>
        </section>
      </div>
    </section>
  );
};

export default Predictions;
